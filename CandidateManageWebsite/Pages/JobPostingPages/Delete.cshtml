@page
@model CandidateManageWebsite.Pages.JobPostingPages.DeleteModel
@{ 
    ViewData["Title"] = "Delete";
    String RoleID = HttpContext.Session.GetString("RoleID");
    if ((RoleID != "1") && (RoleID != "2"))
    {
        Response.Redirect("/NoPermission");
    }
}
